<header>
  <h1>All Trips</h1>
</header>

<section>
  <table class="list-of-data">
    <tr class="table-header details">
      <td>ID</td>
      <td>Driver</td>
      <td>Passenger</td>
      <td>Date</td>
      <td>Cost</td>
      <td>Rating</td>
      <td></td>
      <td></td>
    </tr>
    <% @trips.each do |trip| %>
      <tr class="details">
        <td><%= link_to "#{trip.id}", trip_path(trip.id) %></td>
        <td><%= trip.driver_id %></td>
        <td><%= trip.passenger_id %></td>
        <td><%= trip.date %></td>
        <td><%= trip.display_cost %></td>
        <td>
          <% if trip.rating %>
            <%= trip.rating %>
          <% else %>
            <%= "--" %>
          <% end %>
        </td>
        <td><%= link_to "Edit", edit_trip_path(trip.id) %></td>
        <td><%= link_to "Delete", trip_path(trip.id), method: :delete,  data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </table>
</section>
